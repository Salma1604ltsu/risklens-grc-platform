import {Link,Route,Routes,useNavigate} from "react-router-dom";import {getDashboard,getMe,login,logout,token} from "./api";import {useEffect,useState} from "react";
const nav=["Dashboard","Risk Register","Compliance","Controls","Evidence","Remediation","Reports","Audit Logs"];
function Login(){const n=useNavigate(),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState("");async function submit(e){e.preventDefault();try{await login(email,password);n("/")}catch(x){setError(x.message)}}return <div className="login-page"><form className="login-card" onSubmit={submit}><div className="brand center">Risk<span>Lens</span><small>GRC PLATFORM</small></div><h1>Sign in</h1><p className="muted">Access your cybersecurity GRC workspace.</p><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<div className="error">{error}</div>}<button>Sign in</button></form></div>}
function Dashboard(){const[d,setD]=useState(null);useEffect(()=>{getDashboard().then(setD)},[]);const c=d?[["Assets",d.assets.total],["Open Risks",d.risks.open],["Critical Risks",d.risks.critical],["Compliance",d.compliance.assessment_rate_percent+"%"],["Pending Evidence",d.evidence.pending_review],["Overdue Remediation",d.remediation.overdue]]:[];return <section><h1>GRC Dashboard</h1><p className="muted">Live cybersecurity risk and compliance posture.</p><div className="grid">{c.map(([l,v])=><div className="card" key={l}><span>{l}</span><strong>{v}</strong></div>)}</div><div className="panel"><h2>Risk Posture</h2>{d?<p>Critical {d.risks.critical} · High {d.risks.high} · Medium {d.risks.medium} · Low {d.risks.low}</p>:<p className="muted">Loading live metrics...</p>}</div></section>}
function Placeholder({title}){return <section><h1>{title}</h1><div className="panel"><p className="muted">Module UI is ready for API integration.</p></div></section>}
function Protected(){const n=useNavigate(),[u,setU]=useState(null);useEffect(()=>{if(!token())return n("/login");getMe().then(setU).catch(()=>{logout();n("/login")})},[]);if(!u)return <div className="loading">Loading...</div>;return <div className="app"><aside><div className="brand">Risk<span>Lens</span><small>GRC PLATFORM</small></div><nav>{nav.map(x=><Link key={x} to={x==="Dashboard"?"/":"/"+x.toLowerCase().replaceAll(" ","-")}>{x}</Link>)}</nav><button className="logout" onClick={()=>{logout();n("/login")}}>Sign out</button></aside><main><header><div><b>Risk Management & Compliance</b><small>Enterprise GRC workspace</small></div><div className="user">{u.role.toUpperCase().replace("_"," ")}</div></header><Routes><Route path="/" element={<Dashboard/>}/>{nav.slice(1).map(x=><Route key={x} path={"/"+x.toLowerCase().replaceAll(" ","-")} element={<Placeholder title={x}/>}/>)}</Routes></main></div>};export default function App(){return <Routes><Route path="/login" element={<Login/>}/><Route path="*" element={<Protected/>}/></Routes>}